// Export types
export type { BaseIconProps, MultiColorIconProps, OutlineIconProps, FilledIconProps, IconComponent } from "./types";

// Export all icon components
export { ExchangeIcon } from "./exchange-icon";

// Export default exports with different names to avoid conflicts
export { default as ExchangeIconDefault } from "./exchange-icon";